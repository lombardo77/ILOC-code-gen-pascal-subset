program main;
var a, b, c: array[1..3] of integer;
    i: integer;
begin
  for i := 1,3 do
    begin
      a[i] := i;
      b[i] := i + 4;
      c[i] := i * 7
    end;
  writeln(a[3]);
  writeln(b[1]);
  writeln(c[2])
end.
